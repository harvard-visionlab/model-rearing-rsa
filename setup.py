from setuptools import setup, find_packages
   
setup(
    name = 'model_rearing_rsa',
    packages = find_packages(),
    install_requires = [
        # 'lightning-sdk',
        # 'litdata @ git+https://github.com/Lightning-AI/litdata.git',
    ]
)